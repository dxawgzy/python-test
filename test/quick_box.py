#!/usr/bin/python

class Server(object):
    def __init__(self):
        self._server_num = 5
        for i in range(self._server_num):
            dir_path = '/tmp/server%s' % I
            if not os.path.isdir





